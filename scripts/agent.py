import fetch
from fetch import Agent as agent
import match
import dragon
from dragon import Dragon as dragon

class Player(agent):
    #player parser agent

class Game(agent):
    #game parser agent

class Match(dragon)):
    #match parser agent


